# 2026-09-04 NFP 真实当期演示执行卡

状态：`A004_PRECOMMIT_INPUT_READY`。BLS 日程表把 2026 年 8 月 Employment Situation 固定在
2026-09-04 08:30 America/New_York（12:30 UTC）。这是当前模型训练与确认所用的同类宏观发布。

根变量固定为 Philadelphia Fed 所保存的非农就业月增量首次发布值，减去前 12 个可用首次发布值的
中位数 `68.5` 千人；尺度固定为锁定算法所得 `124.42677605270522` 千人。它是参考类预测，
不是市场调查共识。2025:09 和 2025:10 没有 provider first-release 值，按冻结代码使用前 12 个可用值，
不插补、不在结果发布后改口径。

## 时间线

- T−1：封存 BLS 日程、Philadelphia Fed 原始文件、根期望、根尺度、窗口、决策规则与 SHA-256；
- 12:30 UTC：从 BLS Employment Situation 首次发布读取 headline total nonfarm payroll monthly change；
- 最迟 12:45 UTC：复制已承诺输入，只增加 `root_actual` 和 `root_actual_source`，执行
  `macro-gold-s seal-demo`；禁止修改期望、尺度、窗口或规则；
- 16:00 ET 后：采集 GLD；H.15 发布后采集 2Y nominal 与 5Y real，写入新的 outcome 文件，绝不改 seal；
- T+2：核验主测量、代理敏感性、外部截断和每跳判定；
- 2027-03-04 前：完成六个月覆盖率/Brier 回查。

## 发布窗口命令

```bash
# 先把 12:30 UTC 后取得的 BLS 官方页面/文本快照保存到以下路径。
# 将 ACTUAL_K 改为官方 headline 非农月增量（千人）。
PYTHONPATH=src .venv/bin/python -m macro_gold_latent.cli prepare-seal-input \
  --event-id NFP_202608_REL_20260904_A004 \
  --actual ACTUAL_K \
  --actual-source-file demo/evidence/NFP_202608_bls_actual_20260904.json \
  --actual-source-url https://www.bls.gov/news.release/empsit.nr0.htm \
  --output demo/seal_inputs/NFP_202608_REL_20260904_A004.json

PYTHONPATH=src .venv/bin/python -m macro_gold_latent.cli seal-demo \
  --input demo/seal_inputs/NFP_202608_REL_20260904_A004.json
```

## 固定决策

只有 90% CI 下沿高于由效用参数计算出的 break-even probability `0.208` 才允许建仓；CI 宽度大于
0.30 必须放弃建仓。precommit 与 seal 入口都会验证书面阈值和执行代码一致。
“不交易”是允许且可审计的模型决策，不得因为希望演示产生仓位而改阈值。

来源：https://www.bls.gov/schedule/news_release/empsit.htm
