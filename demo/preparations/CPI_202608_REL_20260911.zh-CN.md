# 2026-09-11 CPI 真实当期演示执行卡

状态：`PRECOMMIT_READY`。截至 2026-09-03，BLS 日程表列出的 2026 年 8 月 CPI 发布时间为
2026-09-11 08:30 America/New_York（12:30 UTC）。

根变量固定为核心 CPI 首次发布环比值减 Cleveland Fed 2026-09-02 核心 CPI 环比 nowcast
`0.203341742698412`；尺度固定为此前 60 次同定义 nowcast error 的样本标准差
`0.13461419765634342` 个百分点。该 nowcast 是模型预测，不称为市场调查共识。

## 时间线

- T−8：保存 Cleveland Fed nowcast、发布时间、根尺度、窗口、决策规则和 SHA-256，并执行不可覆盖的
  `precommit-demo`；
- 12:30 UTC：读取 BLS first release actual，计算 `root_z=(actual-consensus)/root_scale`；
- 最迟 12:45 UTC：只补充 `root_actual` 与 `root_actual_source`，执行 `macro-gold-s seal-demo`；程序会
  拒绝任何被修改的事前字段；
- 16:00 ET 后：采集主/代理终点，但只能写新 outcome 文件，不能修改 seal；
- T+2：核验 H.15、GLD、外部事件并运行逐跳判定；
- 2027-03-11 前：完成六个月覆盖率/Brier 回查。

## 固定决策

只有 90% CI 下沿高于 break-even probability 才允许建仓；CI 宽度大于 0.30 不建仓。其他情况按
报告中的保守仓位缩放。演示失败、数据缺失或外部截断均保留，不顺延挑选“表现更好”的下一次发布。

来源：https://www.bls.gov/schedule/news_release/cpi.htm
