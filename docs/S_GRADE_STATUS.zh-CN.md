# S 档状态与下一动作

## 已通过

- 独立仓库、配置、固定依赖、运行/测试脚本和可再分发官方原始输入已经齐备；干净 Git 克隆复现报告
  将在首次提交后生成，并作为单独硬门；
- 164 条确认链，22 条终点成功；Brier 0.1193 `<0.15`；
- 动态条件潜变量链优于题目要求的朴素边际连乘（0.1193 vs 0.1284）；
- 逐跳成功率 0.348 → 0.238 → 0.134；可靠性表和图已生成；
- 真实认识论 CI；隐藏真值 oracle 的 90% CI 覆盖率 0.920；
- CI 宽度—实际误差 Spearman ρ=0.721，p=0.0002，且宽组误差更高；
- 证据 30%→60% 时中心沿证据上移、CI 收窄；
- 外部截断与内生断裂分开；每跳 p_i/n_i、来源事件、系数/协方差/随机种子可复算；
- 随机三跳复算最大误差 0；EVSI/CI/盈亏阈值停止规则可在代码触发；首个非 `CONTINUE` 状态成为
  实际停止点，后续跳明确标为反事实审计；
- 每个前缀均输出参数、测量、制度漂移、共同 frailty 与外部截断的方差来源比；
- 数据质量、源哈希、事前尺度和协议锁链审计通过；修订锁有可验证 RFC 3161 时间戳。
- 2026-09-04 NFP 已完成发布前双阶段承诺：参考类期望、根尺度、模型/数据哈希、窗口和决策规则不可覆盖；
  Amendment 002 将协议从写死 CPI 改回题面允许的宏观范围；Amendment 003 删除题面未要求的签署
  硬门并把 climatology skill 保留为诊断；Amendment 004 在发布前修复书面 `0.26` 与执行 `0.208`
  的阈值冲突。最终选定记录 `NFP_202608_REL_20260904_A004` 的
  precommit SHA-256 为 `4a845ee0bb55c1ab4dd01aabc69c80d8be9f04e44a4f127a498514a2440c7783`，
  DigiCert RFC 3161 时间戳验证通过。
- 原 CPI、首份 NFP、A002 和阈值不一致的 A003 precommit 均未删除，已标记为
  `WITHDRAWN_NOT_USED_FOR_SUBMISSION`，不计为选定演示。

## 仍未通过，因此当前不能称 S-ready

1. **干净 Git 克隆复现**：首次提交后必须从远程克隆，不带本机 `.venv` 或 Yahoo raw，执行固定依赖安装、
   `run --offline`、全部测试和时间戳核验，并写入 `submission_package_audit.json`；
2. **真实当期演示**：选定 NFP 于 2026-09-04 12:30 UTC 发布。发布前承诺已经完成，但 actual 尚未发布，
   因此第二阶段 root seal 不能提前执行，更不能用历史事件冒充。

## 非硬门但必须披露

- 主模型 Brier 0.11928，climatology 0.11862，skill=-0.0056；相对 climatology 的年份区块
  bootstrap 90% CI 为 `[-0.00059, 0.00195]`，不能证明更优；相对朴素连乘的差为
  `[-0.01422, -0.00414]`，改进稳定。
- 平均预测 0.0698，实际三跳率 0.1341，存在整体低估；不能在同一确认集事后修正。
- 操作者可访问历史原始价格，Amendment 001 也发生在标签打开后，因此 confirmation 不是最强的
  第三方托管 pristine holdout。Amendment 003 删除的是自定义签署硬门，不删除这些事实或主办方审查权。

## 合法的下一步

- 2026-09-04 对 A004 按执行卡在 12:30–12:45 UTC 通过 `prepare-seal-input` 只补
  `root_actual/root_actual_source` 并完成第二阶段 seal；
- 当日收盘及 H.15 数据可用后，追加 outcome 文件并在 T+2 完成逐跳主测量、代理和外部截断复核；
- 将当前 2011–2024 confirmation 永久关闭为评估集。若要改进 climatology skill，只能冻结 v2 后使用
  新的、未查看的数据（授权盘中样本或未来逐月样本），不能继续优化这 164 条；
- 新外部批必须通过 `data/contracts/external_confirmation/` 的接入器：至少 50 个新事件、逐事件预测早于
  label unlock、零事件 ID 重叠，且解锁前出现标签文件即失败；
- seal 后冻结提交包、运行隔离复现并发布总 SHA-256 清单；主办方可在正常评审中判断日频确认集和
  Amendment 001 的证据充分性，无需填写参赛者自定义签署模板。
