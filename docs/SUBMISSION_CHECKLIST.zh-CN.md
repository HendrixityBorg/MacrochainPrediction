# 提交清单

本清单用于确认交付物是否齐全。机器明细见 `reports/s_grade_gate.json`，数值以可重复生成的 JSON/CSV
产物为准。

## 工程与复现

- [x] 完整仓库、README、依赖、配置样例、运行脚本、测试脚本和 Dockerfile；
- [x] 离线模式只读取冻结数据，不访问网络、不重建确认标签；
- [x] 输出链级概率、逐跳 `p_i/n_i`、90% CI、停止轨迹、不确定性来源和评估报告；
- [x] 干净克隆复现记录、协议时间戳和关键文件哈希可核验；
- [x] 中文 README、方法、数据、失败边界、当期演示和总报告。

## 模型与比较

- [x] 使用 `P(E1)P(E2|E1)P(E3|E1,E2)`，不把三个无条件频率直接相乘；
- [x] 传播测量误差、代理相关、制度漂移、共同 frailty 和外部截断；
- [x] 内生断裂与外部截断分别输出；
- [x] 主模型 Brier 0.1193，朴素边际连乘为 0.1284；
- [x] 每跳来源事件、特征、系数、协方差、随机种子、n_raw 与 n_eff 可查；
- [x] 最小条件风险集为 16，未触发 `<5` 的稀有事件回退。

## 停止与区间

- [x] 每个前缀输出 CI、盈亏平衡概率、EVSI、复核成本和停止原因；
- [x] 首个非 `CONTINUE` 状态写入 `execution_stop`；
- [x] 停止后的跳标为反事实，仍保留供复算；
- [x] 90% CI 来自参数、测量、相关性、制度漂移和截断概率传播；
- [x] 合成隐藏真值覆盖率 0.920；
- [x] 历史链 CI 宽度—误差 ρ=0.721，p=0.0002；
- [x] 证据增强实验的中心和区间方向符合更新预期。

## 确认评估

- [x] 164 条有效确认链；
- [x] Brier 0.1193；
- [x] 可靠性图、10 桶表和 Wilson 90% 区间；
- [x] 逐跳实际率 0.348 → 0.238 → 0.134；
- [x] 1–6 跳合成 oracle 分别报告覆盖率；
- [x] 失败案例和边界条件没有从报告中移除。

## 真实当期演示

- [x] 事件链为 NFP 首次发布创新 → 政策路径 → 实际利率 → 黄金；
- [x] 发布前固定期望、尺度、模型/数据哈希、结果窗口和交易规则；
- [x] BLS actual 与 `root_z` 推导已记录；
- [x] 逐跳 `p_i/n_i`、链概率、90% CI、不确定性来源和第一跳停止已记录；
- [x] 包含预测的 Git commit 早于最早下游结果窗口完成；
- [x] 交易动作是规则生成的 `NO_POSITION`；
- [x] 下游标签维持未决，不并入 164 条确认集；
- [x] 六个月 outcome、Brier 与覆盖率追踪计划已经给出。

## 提交前最后检查

- [x] 在内容 commit `6b7c409` 的远端浅克隆中重新运行 `./run.sh --offline` 与 `./test.sh`；
- [x] `reports/SUBMISSION_MANIFEST.json` 已记录内容 commit、验证命令和核心文件哈希；
- [x] 内容 commit 已推送且远端仓库可以克隆；
- [ ] 以 `README.md` 为评审入口，不把内部历史失败记录放在首屏；
- [ ] 后续 outcome 只能新增，不得覆盖当期预测。
