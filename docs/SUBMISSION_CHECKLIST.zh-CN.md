# SX-CH-001 提交清单

本清单按题面 S 档条款组织。机器明细见 `reports/s_grade_gate.json`；若二者不一致，以原始产物和可重复
命令为准，不以文字声明覆盖失败门。

## 1. 工程可运行

- [x] 独立 Git 仓库、`README.md`、`pyproject.toml`、`requirements.txt` 与固定版本
  `requirements.lock.txt`；
- [x] `config/default.json`、`setup.sh`、`run.sh`、`test.sh`、Dockerfile 与 `.dockerignore`；
- [x] 冻结事件记录和可再分发的官方原始输入；Yahoo 原始长历史不提交；
- [x] 从 GitHub commit `07a10a4` 干净克隆，以 Python 3.11 全新环境完成离线重建、18/18 测试、
  协议与 A004 时间戳核验；机器证据见 `reports/submission_package_audit.json`；
- [x] 链级概率、逐跳 `p_i/n_i`、90% CI、逐前缀停止、不确定性来源和评估报告。

## 2. 非朴素模型

- [x] `P(E1)P(E2|E1)P(E3|E1,E2)` 条件链，而非无条件边际连乘；
- [x] 跨跳共同 frailty、代理残差相关与外部截断 Monte Carlo；
- [x] 通胀预期、美元和安全港/遗漏冲击单独披露；
- [x] 主模型 Brier 0.1193，朴素连乘 0.1284；
- [x] 每跳来源事件、特征、系数、协方差、随机种子、n_raw 与 n_eff；
- [x] 随机三跳复算最大误差 0；
- [x] 最小条件风险集为 16，未触发 `<5` 稀有事件 fallback；对应 ≥20 附加条款不适用。

## 3. 停止规则

- [x] CI 上下沿、盈亏平衡概率、EVSI、复核成本和最大跳数保护栏；
- [x] 首个非 `CONTINUE` 状态写入 `execution_stop`；
- [x] 停止后的前缀标为 `counterfactual_after_stop`，仍保留供三跳审计；
- [x] 每个判断均输出机器可读原因。

## 4. 校准与 CI

- [x] 164 条有效确认链，22 条终点正例；
- [x] Brier 0.1193 `<0.15`；
- [x] 可靠性图、10 桶表及 Wilson 90% 区间；
- [x] 逐跳实际率 0.348 → 0.238 → 0.134；
- [x] CI 传播参数、测量、相关性、制度漂移和外部截断，不使用跳点乱序；
- [x] 合成隐藏 test truth 的 90% CI 覆盖率 0.920；
- [x] 历史链 CI 宽度—误差 ρ=0.721，p=0.0002，宽组误差高于窄组；
- [x] 证据从 30%/n=20 增强到 60%/n=200 时，中心上移且 CI 收窄；
- [x] 1–6 跳 oracle 按跳数分别报告覆盖率。

## 5. 报告

- [x] 中文 README、方法、数据、失败边界、协议、状态与总报告；
- [x] 公式、参数估计、实验边界、baseline、结果、误差来源、局限和扩展方向；
- [x] 最大误差事件和典型边界条件；
- [x] A004 中文失败关闭记录已经纳入总报告；
- [ ] 新的前瞻事件封存后自动生成合格中文真实演示运行记录并纳入总报告。

## 6. 真实当期演示

- [x] 真实宏观链：NFP 首次发布创新 → 政策路径 → 实际利率 → 黄金；
- [x] A004 在发布前固定期望、尺度、模型/数据哈希、窗口和交易规则；
- [x] 书面与执行盈亏阈值均为 0.208；A003 因阈值冲突被保留但撤回；
- [x] A004 precommit 与协议均有可验证 RFC 3161 时间戳；
- [x] A004 错过 2026-09-04 12:30–12:45 UTC 封存窗口，已按 fail-closed 拒绝且未回填；
- [ ] 为新的尚未发布宏观事件重新预承诺；
- [x] A004 失败记录、BLS 截止后 actual 交叉核对与不可计入门禁的反事实诊断已完整保存；
- [ ] 对新的前瞻事件生成 actual→root_z→p_i→链 CI→停止→交易决策的完整中文记录；
- [x] 六个月 outcome、Brier 与覆盖率追踪计划。

## 必须持续披露的负结果

- 主模型略差于 climatology、primary-only 与 direct terminal logistic；
- 平均预测低于确认集实际三跳率，存在整体低估；
- 日频窗口混入同时新闻，不能单独识别结构因果；
- 操作者能够接触历史原始价格，confirmation 弱于第三方托管的真正前瞻 holdout；
- 外部截断 Beta(1,19) 是锁定的弱正则先验，必须同时提供中心敏感性。
